<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="200dp"
    android:layout_height="40dp"
    android:background="@drawable/bg_round_corners_25_white">

    <TextView
        android:id="@+id/text_back_left"
        android:layout_width="98dp"
        android:layout_height="36dp"
        android:layout_marginStart="2dp"
        android:gravity="center_vertical|center_horizontal"
        android:textColor="@color/dark_grey"
        android:textSize="16sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:visibility="visible"
        tools:text="LeftText" />

    <TextView
        android:id="@+id/text_back_right"
        android:layout_width="98dp"
        android:layout_height="36dp"
        android:layout_marginStart="2dp"
        android:gravity="center_vertical|center_horizontal"
        tools:text="RightText"
        android:textColor="@color/dark_grey"
        android:textSize="16sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <FrameLayout
        android:id="@+id/toggle_custom_switch"
        android:layout_width="98dp"
        android:layout_height="36dp"
        android:layout_marginStart="2dp"
        android:background="@drawable/bg_round_corners_25_dark_grey"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" >

        <TextView
            android:id="@+id/text_toggle"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:textColor="@color/white"
            android:textSize="16sp"
            android:textStyle="bold"
            android:gravity="center_vertical|center_horizontal"
            tools:text="LeftText"/>

    </FrameLayout>

</androidx.constraintlayout.widget.ConstraintLayout>